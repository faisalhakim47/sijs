{
  "name": "sijs",
  "version": "1.1.0",
  "description": "Sijs",
  "main": "si.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild si.js --bundle --minify --outfile=si.min.js && esbuild si.global.js --bundle --minify --outfile=si.global.min.js --target=chrome45"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/faisalhakim47/sijs.git"
  },
  "author": "Faisal Hakim <faisalhakim47@gmail.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/faisalhakim47/sijs/issues"
  },
  "homepage": "https://github.com/faisalhakim47/sijs#readme",
  "devDependencies": {
    "esbuild": "^0.12.21"
  }
}
